# create_ebs_user.yaml

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wangzg-ebs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 100Gi